# Server Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# JWT Configuration (CHANGE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# Gmail OAuth2 Configuration
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Gmail API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 5. Set authorized redirect URI to: http://localhost:3000/api/gmail/callback
GMAIL_CLIENT_ID=your-gmail-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-gmail-client-secret
GMAIL_REDIRECT_URI=http://localhost:3000/api/gmail/callback

# Security
BCRYPT_ROUNDS=12
CORS_ORIGIN=*

# User Whitelist (comma-separated, leave empty to allow all)
ALLOWED_EMAILS=user1@gmail.com,user2@gmail.com,user3@gmail.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# PostgreSQL Database
# Railway provides DATABASE_URL automatically when you add a Postgres plugin
# For local development:
DATABASE_URL=postgresql://user:password@localhost:5432/email_tracker

# Cloudflare R2 (for tracked file attachments)
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=buggers
R2_PUBLIC_URL=https://your-r2-public-domain.com
